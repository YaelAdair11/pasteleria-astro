---
import '../styles/login.css'; // Mismo estilo

const error = Astro.url.searchParams.get('error');
---
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale-1.0" />
  <title>Staff - Iniciar Sesión</title>
</head>
<body>
  <div class="login-container">
    <h1>Acceso Staff</h1>
    <h2>Iniciar Sesión</h2>

    {/* Apunta a la nueva API de staff */}
    <form method="POST" action="/api/auth/staff-login"> 
      <div class="input-group">
        <label for="username">Usuario</label> {/* Pide Username */}
        <input type="text" id="username" name="username" placeholder="Ingresa tu usuario" required />
      </div>

      <div class="input-group">
        <label for="password">Contraseña</label>
        <input type="password" id="password" name="password" placeholder="Ingresa tu contraseña" required />
      </div>

      <button type="submit" class="btn">Ingresar</button>
    </form>

    {error && <p id="message" style="color: red;">{error}</p>}

    {/* (Opcional) Enlace para ir al login de clientes */}
    {/* <p style="margin-top: 1rem;"><a href="/login">Acceso Clientes</a></p> */}
  </div>
</body>
</html>